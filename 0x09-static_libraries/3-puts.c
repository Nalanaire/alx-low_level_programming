#include "main.h"
void_puts(char *str)
#include <stdio.h>
/**
 * main - entry point
 * Return: always 0
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
